version: "3.9"
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: hans
      POSTGRES_PASSWORD: hans
      POSTGRES_DB: hans_db
    ports:
      - "5432:5432"
    volumes:
      - hans_pg:/var/lib/postgresql/data

volumes:
  hans_pg:
